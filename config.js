'use strict';
const GAME_CONFIG = Object.freeze({
  version: 'v0.30 PROJECT REFACTOR',
  canvas: { width: 1000, height: 600 },
  match: { seconds: 75, scoreToWin: 3 },
  repository: 'slime-kickoff'
});
